// DigiCoin Root Environment Configuration

// Frontend
const FRONTEND_URL = 'http://localhost:5173';

// Backend
const BACKEND_URL = 'http://localhost:3001';
const PORT = 3001;

// Note: The actual backend server is in the /backend folder
// Run: npm run backend:dev (from root) or cd backend && npm run dev

module.exports = { FRONTEND_URL, BACKEND_URL, PORT };

